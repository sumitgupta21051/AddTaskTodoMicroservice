apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: add-task-ingress
  annotations:
    kubernetes.io/ingress.class: addon-http-application-routing  
spec:
  ingressClassName: nginx
  rules:
  - host: addtask.devops-engineer-adda.store
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: add-task
            port: 
              number: 80
